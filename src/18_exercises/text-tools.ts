export namespace TextTools {
  export function toUpperCase(text: string) {
    return text.toUpperCase();
  }
  export function reverse(text: string) {
    return text.toLowerCase();
  }
  export const textConstant = "Das ist eine Konstante";
}
